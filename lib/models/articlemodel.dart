class ArticleModel{
  String author;
  String title;
  String description;
  String url;
  String urlToImage;
  String content;
  DateTime publshedAt;
  

  ArticleModel({this.author, this.description, this.content, this.url,this.title, this.urlToImage,this.publshedAt });
}

